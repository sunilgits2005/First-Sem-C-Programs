#include<stdio.h>
char main()
{
char c;
printf("Enter an alphabet:");
scanf("%c", &c);
if
{
char upperc = ;
printf("The uppercase version is: %c", upperc);
}
else
{
printf("The entered alphabet is already in uppercase.");
}
printf("The entered character is not even an alphabet. Good Joke.");
}
